# Njangi Backend

Fastify + Prisma + Neon (Postgres)

## Requirements
- Node.js (recommended: LTS)
- A Postgres database (Neon)
- Prisma CLI (installed via devDependencies)

## Setup
1) Install deps
```bash
npm install
